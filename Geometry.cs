
namespace Polymorphism
{
    public class Geometry
    {
        /*polymorphism uses methods to preform different tasks when it occurs
         that it has related classes*/
        //the parent class and "parent" method 
        //uses viturel to be able to override and reuse the method and change it 
        public virtual void Area()
        {
            double area = 0;
        }

        /*take many forms (real life example):
        one person kan be a costumer, an employe, a parent, a sibling etc*/
    }
}
